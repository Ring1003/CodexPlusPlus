{
  "identifier": "default",
  "description": "Default permissions for the Codex++ Manager main window. Grants the dialog plugin's default permission set so the file/folder picker can be opened from settings (fixes #345). Also grants updater/process/shell permissions for online auto-update and launcher sidecar execution.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "updater:default",
    "process:default",
    "process:allow-restart",
    "process:allow-exit",
    "shell:allow-execute",
    "shell:allow-open"
  ]
}
